require 'cinch'

bot = Cinch::Bot.new do
  configure do |c|
    c.server = "localhost"
    c.channels = ["#op"]
    c.nick = 'cat'
  end

  on :message do |m|
    m.reply "#{m.channel.name} #{m.user.nick} #{m.message}"
  end
end

bot.start
